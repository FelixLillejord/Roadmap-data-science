# Core HTTP client
requests>=2.31

# HTML parsing (choose one stack; defaulting to selectolax)
selectolax>=0.3.17
# beautifulsoup4>=4.12  # alternative
# lxml>=5.2            # alternative parser backend for bs4

# Data processing + parquet
pandas>=2.2
pyarrow>=15

# Text normalization
charset-normalizer>=3.3

# Optional fuzzy matching for org names
# rapidfuzz can be omitted if exact/heuristic matching suffices
rapidfuzz>=3.9

# Testing
pytest>=8.2

